[package]
name = "project_03_mutex"
version = "0.0.1"
license.workspace = true
edition.workspace = true

[dependencies]
tokio = { version = "1.40", features = ["rt-multi-thread", "sync", "time", "macros"] }
